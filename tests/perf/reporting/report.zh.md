# CubeSandbox 核心操作性能基准测试报告

> **生成时间**：2026-07-23 02:47:10 UTC &nbsp;|&nbsp; **SDK**：v0.5.0 &nbsp;|&nbsp; **成功率**：100%

> **重要说明**：所有测试数据与测试环境、测试场景高度相关。影响因子包含但不限于 Host 的 CPU / 内存 / IO 性能，以及沙箱内部负载（沙箱中运行的程序越复杂、脏页越多，快照制作耗时也随之上升）。实际部署时请结合自身硬件和负载情况进行评估。本报告由 `tests/perf` 套件自动生成。

---

## 一、测试环境

### 1.1 硬件信息

| 项目 | 详情 |
|:---|:---|
| **主机名** | `VM-79-34-tencentos` |
| **机器类型** | 腾讯云 SA2.4XLARGE32 |
| **操作系统** | Linux（TencentOS Server 4.4） |
| **内核版本** | `6.6.69-opencloudos9.cubesandbox.pvm.host-gb85200d80fa2` |
| **架构** | x86_64 |
| **CPU 型号** | AMD EPYC 7K62 48-Core Processor |
| **CPU 配置** | 1 Socket × 16 Core = **16 逻辑核** |
| **NUMA 节点** | 1 |
| **内存总量** | **31 GiB**（RAM） |
| **数据盘** | 100 GB HDD（N/A），文件系统 xfs |

### 1.2 沙箱规格与模板

| 项目 | 详情 |
|:---|:---|
| **规格** | cubebox |
| **测试镜像** | `cube-sandbox-cn.tencentcloudcr.com/cube-sandbox/sandbox-code:latest` |
| **模板 ID** | `tpl-68f6b358fca5426baefe46a7`（状态：`READY`） |
| **存储方式** | CoW reflink（xfs） |
| **内存追踪** | soft-dirty（`/proc/PID/clear_refs`） |
| **API 地址** | `http://9.135.79.34:3000` |

### 1.3 组件版本

| 组件 | 版本 |
|:---|:---|
| **CubeAPI** | `v0.5.1`（commit `a164417f`，构建于 2026-07-11T08:09:01Z） |
| **CubeMaster** | `v0.5.1` |
| **Cubelet** | `v0.5.1` |
| **CubeShim** | `v0.5.1` |
| **Guest Image** | `v0.5.1` |
| **节点内核** | `6.6.119-49.6` |
| **Python** | CPython 3.11.6 |
| **SDK** | v0.5.0 |

### 1.4 指标说明

| 指标 | 含义 |
|:---|:---|
| **avg** | 多轮采样的平均单次延迟 |
| **min** | 最小值 |
| **p50 / p95 / p99** | 第 50 / 95 / 99 百分位延迟 |
| **max** | 最大值 |
| **wall** | 整批操作的端到端墙钟耗时（并发场景） |
| **单次均摊 (per)** | 单次操作均摊耗时（wall ÷ 采样数） |

> 所有时间单位均为**毫秒（ms）**。每场景计时前执行 Warm-up（首轮结果丢弃），消除冷启动干扰。每场景 **1** 轮采样。

---

## 二、Create sandbox baseline.

> **测试方式**：创建沙箱

| 并发 | 采样数 | avg(ms) | min(ms) | p50(ms) | p95(ms) | p99(ms) | max(ms) | wall(ms) | 单次(ms) |
|:---:|:---:|:---:|:---:|:---:|:---:|:---:|:---:|:---:|:---:|
| 1 | 1 | 1815.0 ms | 1815.0 ms | 1815.0 ms | 1815.0 ms | 1815.0 ms | 1815.0 ms | 1815.0 ms | 363.0 ms |

**关键结论：**

- 单并发次延迟约 **1815.0 ms**（min 1815.0 / p95 1815.0）

---

## 三、总结

- **性能压测**：共采集 **1** 条场景数据点
- **测试配置**：每场景 1 轮，密度上限 100，成功率 **100%**

---

_本报告由 `tests/perf` 套件自动生成 &nbsp;|&nbsp; CubeSandbox Python SDK v0.5.0_
